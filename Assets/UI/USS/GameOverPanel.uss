/* ===== GameOverPanel.uss ===== */
@import url("Shared.uss");

#GameOverPanel {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    justify-content: center;
    align-items: center;
    flex-direction: column;
    background-color: var(--color-bg-root);
}

#Vignette {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    background-image: resource("Images/ui_vignette");
    background-size: cover;
}

#CardShadow {
    position: absolute;
    width: var(--panel-shadow-width);
    height: var(--panel-shadow-height);
    left: 50%;
    top: 50%;
    margin-left: -400px;
    margin-top: -368px;
    background-image: resource("Images/ui_shadow");
    -unity-slice-left: var(--slice-panel);
    -unity-slice-right: var(--slice-panel);
    -unity-slice-top: var(--slice-panel);
    -unity-slice-bottom: var(--slice-panel);
    opacity: var(--opacity-medium);
}

#Card {
    width: var(--panel-card-width);
    height: var(--panel-card-height);
    flex-direction: column;
    align-items: center;
    background-color: var(--color-card-face);
    border-radius: var(--radius-m);
    opacity: 0;
    scale: var(--panel-start-scale);
    transition: opacity var(--panel-in-duration) var(--panel-easing), scale var(--panel-in-duration) var(--panel-easing);
}

#Card.visible {
    opacity: 1;
    scale: 1;
}

#TopAccent {
    width: var(--panel-card-width);
    height: 7px;
    flex-shrink: 0;
    background-color: var(--color-accent-red);
    border-top-left-radius: var(--radius-m);
    border-top-right-radius: var(--radius-m);
}

#SkullIcon {
    width: 72px;
    height: 72px;
    margin-top: var(--space-xxl);
    flex-shrink: 0;
    background-image: resource("Icons/icon_skull");
    -unity-background-image-tint-color: var(--color-accent-red);
    background-size: contain;
    background-repeat: no-repeat;
    background-position: center;
}

#Title {
    font-size: var(--fs-display);
    -unity-font: var(--font-display);
    -unity-text-align: middle-center;
    letter-spacing: 6px;
    width: 720px;
    height: 130px;
    margin-top: var(--space-l);
    flex-shrink: 0;
    color: var(--color-title-top);
    -unity-text-outline-color: var(--color-title-bottom);
    -unity-text-outline-width: var(--shadow-text-md);
}

#Divider {
    width: 620px;
    height: 2px;
    flex-shrink: 0;
    margin-top: var(--space-s);
    background-color: var(--color-divider);
}

#StatWell {
    width: var(--stat-well-width);
    height: var(--stat-well-height);
    flex-direction: column;
    justify-content: center;
    align-items: center;
    margin-top: var(--space-xxl);
    flex-shrink: 0;
    background-color: var(--color-stat-well-bg);
    border-radius: var(--radius-s);
}

.stat-text {
    font-size: var(--fs-xl);
    -unity-font: var(--font-body);
    color: var(--color-text-primary);
    -unity-text-align: middle-center;
    width: 100%;
    height: 36px;
    flex-shrink: 0;
    margin: var(--space-xs) 0;
}

.stat-text.gold {
    color: var(--color-text-gold);
}

.gameover-button {
    width: var(--button-width);
    height: var(--button-height);
    flex-shrink: 0;
    margin: var(--space-xs) 0;
    font-size: var(--fs-xl);
    -unity-font: var(--font-body);
    color: var(--color-text-primary);
    -unity-text-align: middle-center;
    background-image: resource("Images/ui_button");
    -unity-slice-left: var(--slice-button);
    -unity-slice-right: var(--slice-button);
    -unity-slice-top: var(--slice-button);
    -unity-slice-bottom: var(--slice-button);
    border-left-width: 0;
    border-right-width: 0;
    border-top-width: 0;
    border-bottom-width: 0;
    background-color: rgba(0, 0, 0, 0);
    transition: scale var(--transition-fast) var(--panel-easing);
}

.gameover-button:hover {
    scale: var(--hover-scale);
}

.gameover-button:active {
    scale: var(--press-scale);
}
